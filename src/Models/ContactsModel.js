export class ContactModel {
  constructor(
    idContact,email,message,statut
  ) {
    this.idContact =idContact;
    this.email = email;
    this.message = message;
    this.statut = statut;
  
  }
}
